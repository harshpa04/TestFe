# Form & Field

A small Vite + React storefront for Jira ticket `SCRUM-5`.

## Run locally

```bash
npm install
npm run dev
```

Run the production build and reducer tests with:

```bash
npm run build
npm test
```

The product catalog is local seed data. Cart state is managed with Redux Toolkit and styling uses Sass.
